procs:
  fr-server:
    shell: "API_PORT=$FR_API_PORT alan run --scope frontend -- yarn workspace fr-server dev"
    autostart: false
  metro:
    shell: "alan run --scope frontend -- yarn workspace alan-mobile dev"
    autostart: false
