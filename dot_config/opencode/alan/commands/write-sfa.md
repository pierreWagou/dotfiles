---
description: Write and post Pierre's weekly SFA to #org_alaner-sfa
---

Write Pierre Romon's weekly SFA (Suns / Fires / A+) and post it to `#org_alaner-sfa` (channel ID: C0LJ49SN5).

Delegate to `alan-skills:sfa` with the following vault-first overrides:

## Vault overrides (apply before invoking alan-skills:sfa)

1. **Read the vault first**: Check `📓 Journal/` for the week's daily notes and previous SFAs, `🚀 Projects/` for project progress, `📅 Meetings/` for notable meetings. Use as additional context on top of what the skill fetches.
2. **Voice**: match `🪞 Personal/Voice Profile.md` at `/Users/pierre.romon/Documents/Alan/🪞 Personal/Voice Profile.md`. Pierre's SFA format:
   ```
   :marmot-wave: Engineering - Onboarding
   :sunny:
   • [what went well]
   :fire:
   • [what was hard or blocking]
   :a+:
   • P1: [top priority]
   • P2: [second priority]
   ```
3. **Identity**: Slack user U0B5H5GPS6T, GitHub pierreWagou, Linear "me", email pierre.romon@alan.eu
4. **Archive**: After posting, save the SFA verbatim as `📓 Journal/YYYY-MM-DD -- Weekly SFA.md` (use today's date).

## Customize freely

The generic SFA logic (fetching Slack, Linear, GitHub, Quill activity for the week) stays in `alan-skills:sfa`. The vault context, voice, and archiving rules above are Pierre's personal layer.
